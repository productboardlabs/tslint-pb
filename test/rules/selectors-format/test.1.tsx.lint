import select from 'select';

select(
  [Store],
  () => {},
);

select.noMemo(
  [Store],
  () => {},
);

select.customMemo(
  [Store],
  () => {},
  () => 'key',
);

select(
  [Store],
  (a: number) => {
    return ':)';
  },
);

select(
  [Store, selector],
  (): boolean => {
    return true;
  },
);
